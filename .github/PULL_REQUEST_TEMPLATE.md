## What this changes

<!-- Briefly: what and why. Link an issue if there is one. -->

## How you tested it

<!-- Local checks are below. Beyond those: which model(s) did you actually run a
generation through? If you touched src/apps/video/frames.js, which browsers did
you check, since it has no automated coverage? -->

## Checklist

- [ ] `yarn lint`, `yarn format:check`, `yarn test` and `yarn build` all pass
- [ ] I ran the tool in a browser and the change works
- [ ] If this adds a Replicate endpoint, `server/proxy.js`'s allowlist is updated
      and the PR explains why the new call is needed
